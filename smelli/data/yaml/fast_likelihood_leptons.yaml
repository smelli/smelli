name: Fast Likelihood Lepton Sector
observables: !include_merge_list
  # tau->nuff and tau->Pnu
  - observables_taucc.yaml
  # a_l
  - observables_g-2.yaml
include_measurements: !include_merge_list
  - measurements_leptons.yaml
